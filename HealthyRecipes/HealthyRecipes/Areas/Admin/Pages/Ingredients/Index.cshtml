@page
@model HealthyRecipes.Web.Areas.Admin.Pages.Ingredients.IndexModel
@{
    ViewData["Title"] = "Ingredient Management";
}

<div class="page-header" style="margin-bottom: 32px;">
    <h1 style="font-size: 2rem; color: var(--admin-primary); margin: 0;">Ingredient Management</h1>
    <p style="color: #666; margin-top: 8px;">View and manage all ingredients</p>
</div>

<div class="admin-card" style="margin-bottom: 24px;">
    <form method="get">
        <div style="display: flex; gap: 12px; align-items: center;">
            <input 
                type="text" 
                name="SearchTerm" 
                value="@Model.SearchTerm"
                placeholder="Search meal plans..." 
                style="flex: 1; padding: 10px 16px; border: 1px solid var(--admin-border); border-radius: 6px; font-size: 0.95rem;"
            />
            <button type="submit" class="btn-primary" style="padding: 10px 24px; white-space: nowrap;">
                🔍 Search
            </button>
            @if (!string.IsNullOrWhiteSpace(Model.SearchTerm))
            {
                <a asp-page="/Ingredients/Index" class="btn-ghost-sm">Clear</a>
            }
        </div>
    </form>
</div>

<div class="admin-card">
    <div style="margin-bottom: 16px;">
        <h2 style="font-size: 1.25rem; margin: 0; color: var(--admin-primary);">
            Ingredients (@Model.Ingredients.Count)
        </h2>
    </div>

    <table class="admin-table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Brand</th>
                <th>CalPer100g</th>
                <th>ProteinPer100g</th>
                <th>CarbsPer100g</th>
                <th>FatPer100g</th>
                <th>Created</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.Ingredients.Any())
            {
                @foreach (var plan in Model.Ingredients)
                {
                    <tr style="@(plan.IsDeleted ? "opacity: 0.5;" : "")">
                        <td>
                            <div>
                                <strong>@plan.Name</strong>
                            </div>
                        </td>
                        <td>@plan.Brand</td>
                        <td style="text-align: center;">@plan.CaloriesPer100g</td>
                        <td style="text-align: center;">@plan.ProteinPer100g</td>
                        <td style="text-align: center;">@plan.CarbsPer100g</td>
                        <td style="text-align: center;">@plan.FatPer100g</td>
                        <td>@plan.CreatedAt.ToString("MMM dd, yyyy")</td>
                        <td>
                            @if (plan.IsDeleted)
                            {
                                <span class="status-badge status-deleted">Deleted</span>
                            }
                            else
                            {
                                <span class="status-badge status-active">Active</span>
                            }
                        </td>
                        <td>
                            <div style="display: flex; gap: 8px;"> 
                                <!-- View on public site -->
                                <a asp-controller="Ingredient" asp-action="Details" asp-route-id="@plan.Id" asp-area=""
                                   class="btn-action" target="_blank">
                                    View
                                </a>

                                <!-- Edit using existing RecipeController.Edit -->
                                <a asp-controller="Ingredient" asp-action="Edit" asp-route-id="@plan.Id" asp-area=""
                                   class="btn-action">
                                    Edit
                                </a>

                                @if (!plan.IsDeleted)
                                {
                                    <!-- Delete using existing RecipeController.Delete -->
                                    <form asp-controller="Ingredient" asp-action="Delete" asp-route-id="@plan.Id" asp-area=""
                                          method="post" style="display: inline;">
                                        @Html.AntiForgeryToken()
                                        <button type="submit" class="btn-action btn-danger"
                                                onclick="return confirm('Are you sure you want to delete this mealplan?');">
                                            Delete
                                        </button>
                                    </form>
                                }
                                else
                                {
                                    <!-- Restore - you'd need to add this action to RecipeController -->
                                    <form asp-controller="Ingredient" asp-action="Restore" asp-route-id="@plan.Id" asp-area=""
                                          method="post" style="display: inline;">
                                        @Html.AntiForgeryToken()
                                        <button type="submit" class="btn-action btn-success">
                                            Restore
                                        </button>
                                    </form>
                                }
                            </div>
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="6" style="text-align: center; padding: 32px; color: #666;">
                        No ingredients found
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<style>
    .btn-primary {
        background: var(--admin-primary);
        color: white;
        border: none;
        border-radius: 6px;
        cursor: pointer;
        font-weight: 500;
        transition: all 0.2s;
    }

    .btn-primary:hover {
        background: var(--admin-secondary);
    }

    .btn-action {
        padding: 6px 12px;
        border-radius: 4px;
        text-decoration: none;
        font-size: 0.875rem;
        font-weight: 500;
        border: 1px solid var(--admin-border);
        background: white;
        color: var(--admin-text);
        cursor: pointer;
        transition: all 0.2s;
        display: inline-block;
    }

    .btn-action:hover {
        background: var(--admin-bg);
        border-color: var(--admin-primary);
        color: var(--admin-primary);
    }

    .btn-danger {
        border-color: #ffdddd;
        color: var(--admin-danger);
    }

    .btn-danger:hover {
        background: #fff5f5;
        border-color: var(--admin-danger);
    }

    .btn-success {
        border-color: #d4ecd4;
        color: var(--admin-secondary);
    }

    .btn-success:hover {
        background: #f0f8f0;
        border-color: var(--admin-secondary);
    }

    .status-badge {
        padding: 4px 12px;
        border-radius: 12px;
        font-size: 0.75rem;
        font-weight: 600;
        display: inline-block;
    }

    .status-active {
        background: #d4ecd4;
        color: #2d5016;
    }

    .status-deleted {
        background: #ffdddd;
        color: var(--admin-danger);
    }
</style>
