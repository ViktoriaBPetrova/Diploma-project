@model HealthyRecipes.Web.ViewModels.Category.CreateCategoryViewModel
@{ ViewData["Title"] = "New Category"; }

<div class="container category-form-container">
    <div class="category-form-wrapper">
        <a asp-action="Index" class="category-form-hint">← Categories</a>
        <h1 class="category-form-heading">New Category</h1>

        <div class="form-card">
            <form asp-action="Create" method="post">
                @Html.AntiForgeryToken()
                <div asp-validation-summary="All" class="validation-summary-errors"></div>

                <div class="form-group">
                    <label asp-for="Name">Category name *</label>
                    <input asp-for="Name" class="form-control" placeholder="e.g. Vegan, Keto, High Protein" />
                    <span asp-validation-for="Name" class="field-validation-error"></span>
                </div>

                <div class="category-form-actions">
                    <button type="submit" class="btn-primary">Create</button>
                    <a asp-action="Index" class="btn-ghost">Cancel</a>
                </div>
            </form>
        </div>
    </div>
</div>

