@model HealthyRecipes.Web.ViewModels.Recipe.CreateRecipeViewModel
@{ ViewData["Title"] = "Create Recipe"; }

<div class="container" style="padding-top:48px; padding-bottom:64px;">
    <div style="max-width:720px; margin:0 auto;">
        <a asp-action="Index" style="color:var(--muted); font-size:0.875rem;">← Back to recipes</a>
        <h1 style="margin:16px 0 8px;">New Recipe</h1>
        <p class="page-subtitle" style="margin-bottom:36px;">Share your healthy creation with the community</p>

        <form asp-action="Create" method="post" id="recipeForm">
            @Html.AntiForgeryToken()
            <div asp-validation-summary="ModelOnly" class="validation-summary-errors" style="margin-bottom:16px;"></div>

            <div style="background:white; border-radius:var(--radius-lg); padding:36px; border:1px solid var(--border); box-shadow:var(--shadow); margin-bottom:24px;">
                <h3 style="margin-bottom:20px;">Basic Info</h3>
                <div class="form-group">
                    <label asp-for="Title">Recipe Title *</label>
                    <input asp-for="Title" class="form-control" placeholder="e.g., Healthy Chicken & Rice Bowl" />
                    <span asp-validation-for="Title" class="field-validation-error"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Info">Description *</label>
                    <textarea asp-for="Info" class="form-control" rows="4" placeholder="Describe your recipe, cooking method, and what makes it special…"></textarea>
                    <span asp-validation-for="Info" class="field-validation-error"></span>
                </div>
                <div class="form-row">
                    <div class="form-group">
                        <label asp-for="PrepTime">Prep time (minutes)</label>
                        <input asp-for="PrepTime" type="number" class="form-control" placeholder="30" />
                    </div>
                    <div class="form-group">
                        <label asp-for="Servings">Servings</label>
                        <input asp-for="Servings" type="number" class="form-control" placeholder="2" />
                    </div>
                </div>
                <div class="form-row">
                    <div class="form-group">
                        <label asp-for="Difficulty">Difficulty</label>
                        <select asp-for="Difficulty" class="form-control" asp-items="Html.GetEnumSelectList<HealthyRecipes.Data.Enums.Difficulty>()">
                            <option value="">— Select —</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label asp-for="ImageUrl">Image URL</label>
                        <input asp-for="ImageUrl" class="form-control" placeholder="https://…" />
                    </div>
                </div>
                <div class="form-group">
                    <label asp-for="VideoUrl">Video URL (optional)</label>
                    <input asp-for="VideoUrl" class="form-control" placeholder="https://youtube.com/…" />
                </div>
            </div>

            <div style="background:white; border-radius:var(--radius-lg); padding:36px; border:1px solid var(--border); box-shadow:var(--shadow); margin-bottom:24px;">
                <h3 style="margin-bottom:20px;">Categories</h3>
                <div style="display:flex; flex-wrap:wrap; gap:10px;">
                    @foreach (var cat in Model.AvailableCategories)
                    {
                        <label style="display:flex; align-items:center; gap:8px; cursor:pointer; background:var(--parchment); padding:8px 14px; border-radius:20px; border:1.5px solid var(--border); font-size:0.9rem;">
                            <input type="checkbox" name="SelectedCategoryIds" value="@cat.Id" style="accent-color:var(--forest);" />
                            @cat.Name
                        </label>
                    }
                </div>
            </div>

            <div style="background:white; border-radius:var(--radius-lg); padding:36px; border:1px solid var(--border); box-shadow:var(--shadow); margin-bottom:32px;">
                <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:20px;">
                    <h3>Ingredients</h3>
                    <button type="button" id="addIngredient" class="btn-ghost" style="font-size:0.875rem;">+ Add ingredient</button>
                </div>
                <div id="ingredientsList">
                    <div class="ingredient-row form-row" style="align-items:flex-end; margin-bottom:12px;">
                        <div class="form-group" style="margin-bottom:0;">
                            <label>Ingredient ID</label>
                            <input type="text" name="Ingredients[0].IngredientId" class="form-control" placeholder="Paste ingredient GUID" />
                        </div>
                        <div class="form-group" style="margin-bottom:0;">
                            <label>Quantity (g)</label>
                            <input type="number" name="Ingredients[0].QuantityInGrams" class="form-control" placeholder="100" step="0.1" min="0.1" />
                        </div>
                    </div>
                </div>
                <p class="form-hint">Nutrition totals will be calculated automatically from ingredient quantities.</p>
            </div>

            <div style="display:flex; gap:12px;">
                <button type="submit" class="btn-primary">Create recipe</button>
                <a asp-action="Index" class="btn-ghost">Cancel</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
<script>
let count = 1;
document.getElementById('addIngredient').addEventListener('click', () => {
    const list = document.getElementById('ingredientsList');
    const div = document.createElement('div');
    div.className = 'ingredient-row form-row';
    div.style.cssText = 'align-items:flex-end; margin-bottom:12px;';
    div.innerHTML = `
        <div class="form-group" style="margin-bottom:0;">
            <label>Ingredient ID</label>
            <input type="text" name="Ingredients[${count}].IngredientId" class="form-control" placeholder="Paste ingredient GUID" />
        </div>
        <div class="form-group" style="margin-bottom:0;">
            <label>Quantity (g)</label>
            <input type="number" name="Ingredients[${count}].QuantityInGrams" class="form-control" placeholder="100" step="0.1" min="0.1" />
        </div>
        <button type="button" onclick="this.parentElement.remove()" class="btn-danger" style="height:42px; width:42px; padding:0; justify-content:center;">✕</button>
    `;
    list.appendChild(div);
    count++;
});
</script>
}
